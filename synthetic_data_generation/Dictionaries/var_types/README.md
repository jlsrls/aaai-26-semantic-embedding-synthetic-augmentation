This folder contains a dictionary with the types of each variable, for each wave, to be inserted in the data generation prompt methodology. Example: a file named `types_wave1.json` containing the following:

```
{
    "var1": "a binary",
    "var2": "an ordinal",
    "var3": "a categorical"
}
```

