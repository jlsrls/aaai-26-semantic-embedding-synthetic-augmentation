This folder contains the samples from each cluster generated from Step 1, for each model. As a random seed was used and sample selection does not involve prompting, the same samples should be selected for each model. There must be one folder for each model, e.g., "claude-sonnet-4" or "gpt-5".

